# assets

Drop `demo.gif` here — a short (~5–10s) loop that shows the core value:

1. Open the Beads pane (ribbon icon).
2. The pane renders real `bd list` output.
3. Tick a checkbox → the issue closes and the list refreshes.

Keep it small (< ~3 MB) so it loads fast in the README and on the Obsidian
plugin page. A 2-column vault (notes left, Beads pane right) reads best.
